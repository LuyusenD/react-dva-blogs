// import { getBlogs, getNewBlogs } from '../services'
export default {

  namespace: 'blogs',

  state: {
  },

  subscriptions: {
    setup({ dispatch, history }) {  // eslint-disable-line
    }
  },

  effects: {
  },

  reducers: {
    save(state, action) {
      return state
    },
  },

};
